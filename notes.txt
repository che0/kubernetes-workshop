https://www.radishlogic.com/kubernetes/running-minikube-in-aws-ec2-ubuntu/


===
GKE create VPC-native (aby se dal spojit s MySQL)
    VPC-native
    private cluster
    needs external master ip
    master ip range can be default
private kube cluster needs cloud nat for the nodes to get out - create cloud nat
    bacha na regiony


kubectl apply -f
 deploy


kubectl run hello --image pn2d/hello-web --port=80 --replicas=1
kubectl expose deploy hello --port 80 --target-port 80 --type=LoadBalancer --load-balancer-ip=35.227.18.251 --name=pub-hello2
kubectl exec hello-678dc98cd7-q8gpk /bin/bash -ti



===
Postup:
1. Deployment tupe webovky na HTTP + LoadBalanceru se statickou IP
2. Tupy backend + pripojeni pres ClusterIP + liveness + readiness

3. Mene tupy backend s redisem jako cache https://kubernetes.io/docs/tutorials/stateless-application/guestbook/
4. Normalni webova aplikace (pod s nginx + uwsgi)
5. MySQL databaze z cloudu, napojeni, secrets
6. Cron joby
7. Ingress + TLS


---
blog: 

===
Finalni webovka:
- mailove remindovatko?
